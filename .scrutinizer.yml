checks:
    php:
        code_rating: true
        duplication: true

    python:
        code_rating: true
        duplicate_code: true

    ruby:
        code_rating: true
        duplicate_code: true

tools:
    external_code_coverage:
        timeout: 12000    # Timeout in seconds.
